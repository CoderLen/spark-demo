package com.liny.spark.dataframe

import scala.collection.mutable.ArrayBuffer

object Test extends App {
  
  val columnNames = new ArrayBuffer[String]()
  println(columnNames.toList.mkString(","))
   
}